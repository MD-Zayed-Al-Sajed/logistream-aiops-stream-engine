apiVersion: 1

providers:
  - name: "LogiStream"
    orgId: 1
    folder: "LogiStream"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /var/lib/grafana/dashboards